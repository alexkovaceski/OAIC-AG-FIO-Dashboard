fastapi
uvicorn[standard]
openpyxl
psycopg2-binary
httpx
pytest
pytest-asyncio
# axoquant-llm is installed from a local clone, not from git+github: the repo is
# private and idc-1 has no GitHub auth, so the URL form fails every pip run
# there. Locally: pip install -e <path-to-your-axoquant-llm-clone>. On deploy,
# scripts/deploy.py installs it editable from /home/algolotl/axoquant-llm (the
# remote clone, kept at the pinned commit 235ce71).
