/* foi-charts.js — ECharts init + filter wiring for the FOI Insights pages.

 * Contract (filled in by Task 3): every `.chartbox` mounts an ECharts figure
 * whose key is its `data-figure` attribute, rendering from
 * window.__pageData.figures[key]; filter dropdowns re-select/group
 * window.__pageData facts only — never summing into a new total the platform
 * did not derive.
 *
 * This placeholder declares the interface and does nothing harmful; Task 3
 * provides the implementation.
 */
(function () {
  window.FoiCharts = { init: function () {}, wireFilters: function () {} };
})();
