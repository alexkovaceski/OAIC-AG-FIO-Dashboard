.venv/
__pycache__/
*.pyc
.idea/
node_modules/
background/jpg/

# ignore xlsx/csv everywhere except the pinned data snapshot
*.xlsx
*.csv
!data/sources/
!data/sources/*.xlsx
!data/sources/*.csv

# runtime lineage ledger (Task 4 firehose; regenerated per boot)
data/generated/lineage.jsonl

# fitted risk artifacts (fit_risk_models.py runs offline on idc-1; never committed)
data/generated/risk/

# session handover notes may carry throwaway credentials; never bulk-added
docs/memories/

# house-style repo link (machine-local junction created by
# house-style/install.ps1; deploy.py ships it, git never tracks it)
.house-style/
